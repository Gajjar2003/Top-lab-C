 // Find a area of rectangle
 
 #include <stdio.h>
 
 main()
 {
 	int area,length,width;
 	printf("Enter a length value:");
 	scanf("%d",& length);
 	printf("Enter a width value:");
 	scanf("%d",& width);
 	
 	area=length*width;
 	printf("Area of rectangle value :%d ", area);
 }
